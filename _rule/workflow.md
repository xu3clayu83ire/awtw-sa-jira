# 工作流程規範（本專案補充）

> 基礎規範沿用工作區共用的 `_rule/workflow.md`，Git 分支與版本標記策略見工作區共用的 `_rule/git-flow.md`。本檔案只記錄本專案獨有的狀態。

## 目前進行中的版本

- `feature/v1-no-notion`：核心流程（需求文件／系統設計文件／開發任務清單產出 + Jira 自動建票），刻意不含 Notion 整合。做完並驗證通過後 merge 進 main，打 tag `v1-no-notion`。
